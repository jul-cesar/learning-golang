Golang is a statically typed, compiled programming language 

Designed by goole in 2007, publicly announced in 2009 and version 1.0 realeased in 2012

Know for its simplicit, efficiency and strng support for concurrent programming

it created to make software developers life "easier"



#Run Golang project or file

to run a golang file we use the command go run and the name of the file, in this case go run main.go


if we want it to be compile it to binary we use go build -o main.go

SLICES

Slices are dynamic arrays, is defined with a certain size under the hood, when we increase the size the compiler double it 